# Installation
```
python -m venv ~/.xonsh-env
~/.xonsh-env/bin/pip install xonsh prompt-toolkit pygments xontrib-abbrevs
```

The shell executable will be in
```
~/.xonsh-env/bin/xonsh
```

#### install with pip, check the imports in the config
xontrib-abbrevs
xontrib
